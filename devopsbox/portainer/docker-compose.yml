version: "3"

services:
    portainer:
      image: 'portainer/portainer-ce:latest'
      container_name: devops_box_portainer
      restart: always

      ports:
      - '9000:9000'
      
      volumes:
        - '/var/run/docker.sock:/var/run/docker.sock'
